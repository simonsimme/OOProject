# Base Dockerfile for Java services
FROM openjdk:17-jre-slim

# Set working directory
WORKDIR /app

# Copy jar file
COPY target/*.jar app.jar

# Expose port (will be overridden in specific Dockerfiles)
EXPOSE 8080

# Run the application
ENTRYPOINT ["java", "-jar", "app.jar"]